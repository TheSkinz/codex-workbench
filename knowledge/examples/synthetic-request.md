# Synthetic Example: Evidence-Bounded Request

> Inspect the supplied public design notes and propose a small documentation
> change. Do not edit files until the proposal is reviewed.

Expected handling:

- classify the request as inspect/propose;
- identify the supplied notes as supporting evidence;
- state missing authority instead of guessing;
- create one proposal record;
- return a no-op if no clear change is supported;
- keep the proposal separate from approved documentation.
