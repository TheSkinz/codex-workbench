# Synthetic Pilot Fixtures

These fixtures exercise the bounded runner without private data, network access,
model calls, or writes to the canonical workbench queues.

- `inbox/` contains capture and research handoffs;
- `proposals/` contains a review candidate;
- drift, consolidation, and health use repository state and contracts rather
  than special input documents.
